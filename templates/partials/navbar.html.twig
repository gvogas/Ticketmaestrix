{# Navbar #}
<nav class="bg-purple-900 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <a href="{{ base_path }}/" class="text-white font-bold text-xl tracking-tight">
                ticketmaestrix
            </a>

            {# Desktop links #}
            <div class="hidden md:flex items-center space-x-8">
                <a href="{{ base_path }}/" class="text-purple-200 hover:text-white font-medium transition flex items-center gap-1.5"><i data-lucide="home" class="w-4 h-4"></i> Home</a>
                <a href="{{ base_path }}/events" class="text-purple-200 hover:text-white font-medium transition flex items-center gap-1.5"><i data-lucide="ticket" class="w-4 h-4"></i> Events</a>
                <a href="{{ base_path }}/map" class="text-purple-200 hover:text-white font-medium transition flex items-center gap-1.5"><i data-lucide="map-pin" class="w-4 h-4"></i> Map</a>
                <a href="{{ base_path }}/profile" class="text-purple-200 hover:text-white font-medium transition flex items-center gap-1.5"><i data-lucide="user" class="w-4 h-4"></i> Profile</a>
            </div>

            {# Mobile hamburger button #}
            <button class="md:hidden text-white" id="mobile-menu-btn" aria-label="Toggle menu">
                <i data-lucide="menu" class="w-6 h-6"></i>
            </button>
        </div>

        {# Mobile menu (hidden by default) #}
        <div class="md:hidden hidden pb-4 space-y-2" id="mobile-menu">
            <a href="{{ base_path }}/" class="block text-purple-200 py-2 flex items-center gap-2"><i data-lucide="home" class="w-4 h-4"></i> Home</a>
            <a href="{{ base_path }}/events" class="block text-purple-200 py-2 flex items-center gap-2"><i data-lucide="ticket" class="w-4 h-4"></i> Events</a>
            <a href="{{ base_path }}/map" class="block text-purple-200 py-2 flex items-center gap-2"><i data-lucide="map-pin" class="w-4 h-4"></i> Map</a>
            <a href="{{ base_path }}/profile" class="block text-purple-200 py-2 flex items-center gap-2"><i data-lucide="user" class="w-4 h-4"></i> Profile</a>
        </div>
    </div>
</nav>
